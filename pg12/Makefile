# Makefile

MODULES = mmlite
PGFILEDESC = "mmlite - Logical Replication output plugin for lightweight multimaster environments"

PG_CONFIG = pg_config
PGXS := $(shell $(PG_CONFIG) --pgxs)
include $(PGXS)
